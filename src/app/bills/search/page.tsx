"use client";

import { SearchBillForUpdate } from "@/components/pages/SearchBillForUpdate";

/** Old searchbillAll.aspx — Search Bill For Update (DPR). */
export default function SearchBillStatusPage() {
  return <SearchBillForUpdate source="DPR" />;
}
